# 🚀 Деплой EventTickets на Render

## Быстрый деплой

### Шаг 1: Подготовка репозитория

1. **Создайте репозиторий на GitHub:**
```bash
git init
git add .
git commit -m "Initial commit: EventTickets v2.0"
git branch -M main
git remote add origin https://github.com/YOUR-USERNAME/event-tickets.git
git push -u origin main
```

2. **Или обновите существующий:**
```bash
git add .
git commit -m "Ready for Render deployment"
git push
```

### Шаг 2: Деплой на Render

#### Вариант A: Автоматический деплой (рекомендуется)

1. Перейдите на [render.com](https://render.com)
2. Нажмите **"New +"** → **"Static Site"**
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name:** `eventtickets` (или любое имя)
   - **Branch:** `main`
   - **Build Command:** оставьте пустым или `echo "No build"`
   - **Publish Directory:** `.` (точка)
5. Нажмите **"Create Static Site"**

#### Вариант B: Через Blueprint (с render.yaml)

1. Перейдите на [render.com](https://render.com)
2. Нажмите **"New +"** → **"Blueprint"**
3. Подключите ваш GitHub репозиторий
4. Render автоматически обнаружит `render.yaml`
5. Нажмите **"Apply"**

### Шаг 3: Настройка (опционально)

После деплоя вы можете:

1. **Настроить домен:**
   - Settings → Custom Domain
   - Добавьте свой домен

2. **Настроить переменные окружения:**
   - Settings → Environment
   - Добавьте нужные переменные

3. **Настроить автодеплой:**
   - Settings → Build & Deploy
   - Auto-Deploy: Yes

## Альтернативные платформы

### Netlify

```bash
# Установите Netlify CLI
npm install -g netlify-cli

# Деплой
netlify deploy --prod
```

Или через веб-интерфейс:
1. Перетащите папку на [netlify.com/drop](https://app.netlify.com/drop)
2. Готово!

### Vercel

```bash
# Установите Vercel CLI
npm install -g vercel

# Деплой
vercel --prod
```

### GitHub Pages

1. Создайте репозиторий на GitHub
2. Settings → Pages
3. Source: Deploy from a branch
4. Branch: main, folder: / (root)
5. Save

Сайт будет доступен по адресу:
`https://YOUR-USERNAME.github.io/event-tickets`

## Проверка деплоя

После деплоя проверьте:

- ✅ Главная страница загружается
- ✅ Админ-панель доступна
- ✅ Переключение языков работает
- ✅ Стили загружаются корректно
- ✅ JavaScript работает
- ✅ Шрифты загружаются

## Troubleshooting

### Проблема: 404 при переходе на /admin.html

**Решение:** Убедитесь, что в настройках Render:
- Publish Directory: `.` (точка, не `./` или `/`)

### Проблема: Стили не загружаются

**Решение:** Проверьте пути в HTML:
```html
<!-- Должно быть относительные пути -->
<link rel="stylesheet" href="styles.css">
<!-- НЕ абсолютные -->
<link rel="stylesheet" href="/styles.css">
```

### Проблема: Шрифты не загружаются

**Решение:** Проверьте подключение к интернету. Google Fonts загружаются с CDN.

## Обновление сайта

После изменений в коде:

```bash
git add .
git commit -m "Update: описание изменений"
git push
```

Render автоматически задеплоит новую версию!

## Мониторинг

### Render Dashboard
- Логи деплоя
- Статус сервиса
- Метрики трафика

### Полезные ссылки
- Dashboard: https://dashboard.render.com
- Документация: https://render.com/docs/static-sites
- Поддержка: https://render.com/support

## Стоимость

**Render Static Sites:**
- ✅ Бесплатно для статических сайтов
- ✅ Автоматический SSL
- ✅ CDN включен
- ✅ Неограниченный трафик

## Следующие шаги

После успешного деплоя:

1. **Добавьте backend** (см. BACKEND_INTEGRATION.md)
2. **Настройте домен**
3. **Добавьте аналитику** (Google Analytics)
4. **Настройте мониторинг**

---

**Готово! Ваш сайт теперь онлайн! 🎉**

URL будет примерно таким:
`https://eventtickets.onrender.com`

или с вашим доменом:
`https://yourdomain.com`

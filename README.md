# 🎫 EventTickets - Система продажи билетов

Современная веб-система для продажи билетов на мероприятия с ручным подтверждением оплаты.

## 🚀 Быстрый старт

1. **Откройте файлы в браузере:**
   - `index.html` - клиентская часть (покупка билетов)
   - `admin.html` - админ-панель (управление заказами)

2. **Или запустите локальный сервер:**
   ```bash
   # Python
   python -m http.server 8000
   
   # Node.js
   npx http-server
   
   # PHP
   php -S localhost:8000
   ```

## 📁 Структура проекта

```
EventTickets/
├── index.html          # Главная страница (клиенты)
├── admin.html          # Админ-панель
├── script.js           # JavaScript клиентской части
├── admin-script.js     # JavaScript админ-панели
├── styles.css          # Стили клиентской части
├── admin-styles.css    # Стили админ-панели
├── ticket-template.html # Шаблон билета
├── data/               # JSON данные
│   ├── events.json     # Мероприятия
│   ├── orders.json     # Заказы (пустой файл)
│   └── settings.json   # Настройки сайта
└── README.md           # Эта инструкция
```

## 📊 JSON файлы

### `data/events.json`
Содержит все мероприятия с поддержкой двух языков (RU/FR):
```json
[
  {
    "id": 1,
    "title": {
      "ru": "Концерт классической музыки",
      "fr": "Concert de musique classique"
    },
    "date": "2025-02-15",
    "time": "19:00",
    "tickets": [...]
  }
]
```

### `data/orders.json`
Файл для сохранения заказов (изначально пустой). Заказы сохраняются в localStorage и дублируются в консоль для просмотра.

### `data/settings.json`
Настройки сайта: название, банковские реквизиты, контакты, шаблоны писем.

## 🎯 Функциональность

### Клиентская часть (`index.html`)
- ✅ Просмотр мероприятий
- ✅ Фильтрация по категориям
- ✅ Выбор билетов и количества
- ✅ Оформление заказа
- ✅ Переключение языков (RU/FR)
- ✅ Адаптивный дизайн

### Админ-панель (`admin.html`)
- ✅ Просмотр всех заказов
- ✅ Подтверждение оплаты
- ✅ Управление статусами заказов
- ✅ Список гостей по мероприятиям
- ✅ Экспорт данных в CSV
- ✅ Статистика и аналитика

## 💾 Хранение данных

Система использует **localStorage** для хранения заказов между сессиями. Данные автоматически синхронизируются между клиентской частью и админ-панелью.

**Для просмотра заказов в JSON формате:**
1. Откройте консоль браузера (F12)
2. Создайте заказ на клиентской части
3. В консоли увидите JSON структуру заказа

## 🔧 Настройка

### Изменение мероприятий
Отредактируйте `data/events.json` и перезагрузите страницу.

### Изменение настроек сайта
Отредактируйте `data/settings.json`:
- `siteName` - название сайта
- `bankDetails` - банковские реквизиты
- `contactInfo` - контактная информация

### Добавление нового мероприятия
```json
{
  "id": 4,
  "title": {
    "ru": "Новое мероприятие",
    "fr": "Nouvel événement"
  },
  "date": "2025-03-01",
  "time": "20:00",
  "location": {
    "ru": "Место проведения",
    "fr": "Lieu de l'événement"
  },
  "category": "music",
  "image": "🎵",
  "tickets": [
    {
      "id": "standard",
      "type": {
        "ru": "Стандартный",
        "fr": "Standard"
      },
      "price": 1500
    }
  ]
}
```

## 🌐 Деплой

### GitHub + Render (рекомендуется)
1. Загрузите код на GitHub
2. Подключите репозиторий к Render.com
3. Выберите "Static Site"
4. Publish Directory: `.` (точка)

### Другие платформы
- **Netlify**: перетащите папку на netlify.com/drop
- **Vercel**: `npx vercel --prod`
- **GitHub Pages**: Settings → Pages → Deploy from branch

## 🔍 Отладка

### Просмотр заказов
```javascript
// В консоли браузера
console.log(JSON.parse(localStorage.getItem('eventTicketsOrders')));
```

### Очистка данных
```javascript
// Очистить все заказы
localStorage.removeItem('eventTicketsOrders');
```

### Экспорт заказов
```javascript
// Экспорт в JSON
const orders = JSON.parse(localStorage.getItem('eventTicketsOrders'));
console.log(JSON.stringify(orders, null, 2));
```

## 📱 Поддержка браузеров

- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

## 🆘 Поддержка

При возникновении проблем:
1. Откройте консоль браузера (F12)
2. Проверьте ошибки в консоли
3. Убедитесь, что файлы загружаются корректно

## 📄 Лицензия

MIT License - свободное использование и модификация.

---

**EventTickets v2.0** - Ваш надежный партнер в мире событий! 🎉


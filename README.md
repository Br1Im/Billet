# 🎫 EventTickets - Система продажи билетов

Современная веб-система для продажи билетов на мероприятия с полноценным backend, базой данных и идеальным адаптивным дизайном.

## ✨ Особенности

- 🎨 **Ультра-современный дизайн** - glassmorphism, градиенты, анимации
- 📱 **Идеальный адаптив** - работает на всех устройствах от 320px до 4K
- 🌍 **Двуязычность** - русский/французский с сохранением выбора
- 🔐 **Безопасность** - JWT аутентификация, rate limiting, хеширование
- 💾 **Надежное хранение** - SQLite база данных + localStorage fallback
- ⚡ **Высокая производительность** - оптимизированные запросы, кеширование
- 🎯 **Touch-friendly** - минимум 44px для всех интерактивных элементов

## 🚀 Быстрый старт

### Автоматический запуск (Windows)
```bash
start-server.bat
```

### Ручной запуск
```bash
cd server
npm install
npm start
```

После запуска:
- **Клиентская часть**: http://localhost:3000
- **Админ-панель**: http://localhost:3000/admin.html
- **API**: http://localhost:3000/api/status

## 🔐 Доступ к админ-панели

- **Логин**: `admin`
- **Пароль**: `admin123`
- Сессия: 24 часа

## 📱 Адаптивный дизайн

### Клиентская часть
- **Desktop (>1024px)**: Полноразмерный дизайн с боковыми эффектами
- **Tablet (768-1024px)**: Оптимизированная раскладка
- **Mobile (≤768px)**: Компактный хедер, вертикальные карточки
- **Small (≤480px)**: Супер компактный режим

### Админ-панель
- **Desktop (>768px)**: Боковой сайдбар (220px) с полной навигацией
- **Mobile (≤768px)**: 
  - Верхний хедер: Логотип + Выход (липкий)
  - Нижняя навигация: Секции с прокруткой (липкая)
  - Полноширинный контент
- **Small (≤640px)**: Карточки вместо таблиц для гостей
- **Tiny (≤480px)**: Минимальные отступы, одноколоночная раскладка

## 🏗️ Архитектура

### Backend Stack
- **Node.js** + **Express.js** - веб-сервер
- **SQLite** - база данных
- **JWT** - аутентификация
- **bcryptjs** - хеширование паролей
- **helmet** - безопасность заголовков
- **express-rate-limit** - защита от DDoS

### Frontend Stack
- **Vanilla JavaScript** - без фреймворков
- **CSS3** - современные возможности (Grid, Flexbox, Backdrop Filter)
- **LocalStorage** - кеширование и fallback
- **Fetch API** - асинхронные запросы

## 📁 Структура проекта

```
EventTickets/
├── server/                    # Backend
│   ├── database/             
│   │   ├── db.js             # Подключение к БД
│   │   └── eventtickets.db   # SQLite база
│   ├── routes/               
│   │   ├── auth.js           # Аутентификация
│   │   ├── events.js         # Мероприятия
│   │   ├── orders.js         # Заказы
│   │   └── settings.js       # Настройки
│   ├── scripts/              
│   │   └── init-db.js        # Инициализация БД
│   ├── server.js             # Основной сервер
│   └── package.json          
├── index.html                # Главная страница
├── admin.html                # Админ-панель
├── ticket-template.html      # Шаблон билета
├── script.js                 # Клиентский JS
├── admin-script.js           # JS админ-панели
├── styles.css                # Стили клиента
├── admin-styles.css          # Стили админки
├── start-server.bat          # Автозапуск
└── README.md                 
```

## 📊 База данных

### Схема SQLite:

**events** - Мероприятия
- id, title_ru, title_fr, description_ru, description_fr
- date, time, location, image, created_at

**ticket_types** - Типы билетов
- id, event_id, name, price

**orders** - Заказы
- id, event_id, customer_name, customer_email, customer_phone
- total_amount, status, created_at

**order_items** - Элементы заказов
- id, order_id, ticket_type, quantity, price

**users** - Администраторы
- id, username, password_hash, created_at

**settings** - Настройки системы
- key, value

**guest_checkins** - Отметки входа
- id, order_id, checked_in_at

## 📱 API Endpoints

### Публичные (без аутентификации)

```
GET    /api/events              # Список мероприятий
GET    /api/events/:id          # Детали мероприятия
POST   /api/orders              # Создание заказа
GET    /api/settings            # Публичные настройки
GET    /api/settings/bank       # Банковские реквизиты
GET    /api/status              # Статус сервера
```

### Административные (требуют JWT токен)

```
POST   /api/auth/login          # Вход в админку
POST   /api/events              # Создание мероприятия
PUT    /api/events/:id          # Редактирование
DELETE /api/events/:id          # Удаление
GET    /api/orders              # Список заказов
PATCH  /api/orders/:id/status   # Изменение статуса
DELETE /api/orders/:id          # Удаление заказа
POST   /api/orders/:id/checkin  # Отметка входа
GET    /api/settings/admin      # Все настройки
PUT    /api/settings            # Обновление настроек
```

## 🎯 Функциональность

### Клиентская часть ✅
- Просмотр мероприятий с сервера
- Выбор и заказ билетов
- Форма оформления заказа с валидацией
- Переключение языков (RU/FR) с сохранением
- Адаптивный дизайн для всех устройств
- Современные анимации и эффекты
- Курсор-следилка (только на desktop)
- Плавная прокрутка и навигация

### Админ-панель ✅
- **Мероприятия**: создание, редактирование, удаление
- **Заказы**: просмотр, фильтрация, управление статусами
- **Гости**: список с отметками входа, экспорт в CSV
- **Настройки**: банковские реквизиты, логотип
- **Аутентификация**: JWT с 24-часовой сессией
- **Адаптивный дизайн**: идеальная работа на мобильных
- **Темная тема**: современный UI

## 🔄 Синхронизация данных

Система использует гибридный подход:

1. **Основное хранение** - SQLite на сервере
2. **Резервное хранение** - localStorage в браузере
3. **Автоматический fallback** - при недоступности сервера
4. **Синхронизация** - при восстановлении соединения

## 🛡️ Безопасность

- ✅ Rate limiting (100 запросов/15 минут)
- ✅ Helmet.js для защиты заголовков
- ✅ CORS настройки
- ✅ Валидация входных данных
- ✅ bcrypt хеширование паролей
- ✅ JWT токены с истечением
- ✅ SQL injection защита (prepared statements)
- ✅ XSS защита

## ⚡ Производительность

- Оптимизированные SQL запросы с JOIN
- Кеширование в localStorage
- Минимальные API вызовы
- Асинхронная загрузка данных
- Debounce для поиска и фильтров
- Lazy loading изображений
- CSS анимации вместо JS

## 🎨 Дизайн-система

### Цвета
- **Primary**: #667eea → #764ba2 (градиент)
- **Secondary**: #f093fb → #f5576c
- **Background**: #0a0e27 (темный)
- **Glass**: rgba(255, 255, 255, 0.1) с backdrop-filter

### Типографика
- **Font**: Inter (Google Fonts)
- **Weights**: 300, 400, 500, 600, 700, 800, 900

### Эффекты
- Glassmorphism
- Анимированные градиенты
- Плавающие орбы
- Курсор-следилка
- Hover эффекты
- Smooth transitions

## 🚀 Деплой

### Render.com (готово)
```yaml
# render.yaml уже настроен
# Просто подключите репозиторий
```

### Локальный деплой
```bash
# Windows
start-server.bat

# Linux/Mac
cd server && npm install && npm start
```

### Docker (опционально)
```dockerfile
FROM node:18
WORKDIR /app
COPY server/package*.json ./
RUN npm install
COPY server/ ./
EXPOSE 3000
CMD ["node", "server.js"]
```

## 🔧 Настройка

### Переменные окружения
```env
PORT=3000
JWT_SECRET=your-secret-key
NODE_ENV=production
```

### Изменение порта
```javascript
// server/server.js
const PORT = process.env.PORT || 3000;
```

### Добавление админа
```javascript
// Через API после запуска
POST /api/auth/register
{
  "username": "newadmin",
  "password": "securepassword"
}
```

## 📞 Troubleshooting

### Порт занят
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Linux/Mac
lsof -ti:3000 | xargs kill -9
```

### База данных не создается
```bash
cd server
node scripts/init-db.js
```

### Ошибки CORS
Проверьте настройки в `server/server.js`:
```javascript
app.use(cors({
  origin: '*', // или конкретный домен
  credentials: true
}));
```

## 📈 Статистика проекта

- **Файлов**: 20+
- **Строк кода**: 5000+
- **API endpoints**: 15+
- **Таблиц БД**: 7
- **Языков**: 2 (RU/FR)
- **Брейкпоинтов**: 4 (1024px, 768px, 640px, 480px)

## 🎉 Готово к использованию!

Система полностью готова к продакшену:
- ✅ Полноценный backend с базой данных
- ✅ Идеальный адаптивный дизайн
- ✅ Безопасность и производительность
- ✅ Документация и примеры
- ✅ Готово к деплою

---

**EventTickets v4.0** - Профессиональная система продажи билетов! 🎫✨

*Создано с ❤️ и вниманием к деталям*


# 🤝 Руководство для контрибьюторов

Спасибо за интерес к проекту EventTickets! Мы рады любому вкладу в развитие проекта.

## 📋 Содержание

- [Как внести вклад](#как-внести-вклад)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Коммиты](#коммиты)
- [Pull Requests](#pull-requests)
- [Баг-репорты](#баг-репорты)
- [Предложения функций](#предложения-функций)

## Как внести вклад

### 1. Fork репозитория

Нажмите кнопку "Fork" в правом верхнем углу страницы репозитория.

### 2. Клонируйте ваш fork

```bash
git clone https://github.com/ваш-username/event-tickets.git
cd event-tickets
```

### 3. Создайте ветку для изменений

```bash
git checkout -b feature/amazing-feature
```

Используйте префиксы:
- `feature/` - новая функциональность
- `fix/` - исправление бага
- `docs/` - изменения в документации
- `style/` - изменения стилей
- `refactor/` - рефакторинг кода
- `test/` - добавление тестов

### 4. Внесите изменения

Следуйте [стандартам кода](#стандарты-кода).

### 5. Commit изменений

```bash
git add .
git commit -m "feat: добавлена новая функция"
```

Следуйте [правилам коммитов](#коммиты).

### 6. Push в ваш fork

```bash
git push origin feature/amazing-feature
```

### 7. Создайте Pull Request

Перейдите на GitHub и создайте Pull Request из вашей ветки в `main` основного репозитория.

## Процесс разработки

### Локальная разработка

1. **Установите зависимости:**
```bash
npm install
```

2. **Запустите локальный сервер:**
```bash
npm start
```

3. **Откройте в браузере:**
```
http://localhost:8000
```

### Тестирование

Перед отправкой PR убедитесь, что:

- ✅ Все страницы загружаются без ошибок
- ✅ Переключение языков работает
- ✅ Формы валидируются корректно
- ✅ Стили применяются правильно
- ✅ Нет ошибок в консоли браузера
- ✅ Адаптивность работает на всех устройствах

### Проверка кода

```bash
# Проверьте HTML
# Откройте https://validator.w3.org/

# Проверьте CSS
# Откройте https://jigsaw.w3.org/css-validator/

# Проверьте JavaScript
# Используйте ESLint (если настроен)
```

## Стандарты кода

### HTML

```html
<!-- Используйте семантические теги -->
<section class="events">
    <h2>Заголовок</h2>
    <article class="event-card">
        <!-- Контент -->
    </article>
</section>

<!-- Добавляйте data-translate для переводимых элементов -->
<h1 data-translate="title">Заголовок</h1>

<!-- Используйте осмысленные классы -->
<button class="btn btn-primary">Кнопка</button>
```

### CSS

```css
/* Используйте CSS переменные */
:root {
    --primary-color: #6366f1;
}

/* Группируйте связанные стили */
.card {
    background: var(--primary-color);
    border-radius: 20px;
    padding: 2rem;
}

/* Используйте mobile-first подход */
.container {
    width: 100%;
}

@media (min-width: 768px) {
    .container {
        width: 750px;
    }
}

/* Добавляйте комментарии для сложных стилей */
/* Glassmorphism эффект */
.glass {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
}
```

### JavaScript

```javascript
// Используйте const/let вместо var
const API_URL = 'https://api.example.com';
let currentLang = 'ru';

// Используйте стрелочные функции
const fetchEvents = async () => {
    // код
};

// Добавляйте JSDoc комментарии
/**
 * Переключает язык интерфейса
 * @param {string} lang - Код языка ('ru' или 'fr')
 */
function switchLanguage(lang) {
    // код
}

// Используйте осмысленные имена
const eventCards = document.querySelectorAll('.event-card');
const isValidEmail = (email) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);

// Обрабатывайте ошибки
try {
    const data = await fetchData();
} catch (error) {
    console.error('Ошибка загрузки данных:', error);
}
```

### Форматирование

- **Отступы:** 4 пробела (не табы)
- **Кавычки:** одинарные `'` для JavaScript, двойные `"` для HTML
- **Точка с запятой:** обязательна в JavaScript
- **Длина строки:** максимум 100 символов
- **Пустые строки:** между логическими блоками

## Коммиты

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Типы коммитов:

- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигурации и т.д.

### Примеры:

```bash
feat(events): добавлена фильтрация по дате
fix(admin): исправлена ошибка при удалении мероприятия
docs(readme): обновлена инструкция по установке
style(css): улучшена адаптивность на мобильных
refactor(script): оптимизирована функция switchLanguage
```

## Pull Requests

### Требования к PR:

1. **Описание:**
   - Что изменено
   - Зачем изменено
   - Как протестировать

2. **Чеклист:**
   - [ ] Код следует стандартам проекта
   - [ ] Добавлены комментарии для сложных участков
   - [ ] Обновлена документация (если нужно)
   - [ ] Протестировано на разных устройствах
   - [ ] Нет конфликтов с main веткой

3. **Скриншоты:**
   - Для изменений UI добавьте скриншоты до/после

### Шаблон PR:

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Новая функциональность
- [ ] Исправление бага
- [ ] Улучшение производительности
- [ ] Рефакторинг
- [ ] Документация

## Как протестировать
1. Шаг 1
2. Шаг 2
3. Ожидаемый результат

## Скриншоты
(если применимо)

## Чеклист
- [ ] Код протестирован
- [ ] Документация обновлена
- [ ] Нет конфликтов
```

## Баг-репорты

Используйте [GitHub Issues](https://github.com/your-username/event-tickets/issues) для сообщения о багах.

### Шаблон баг-репорта:

```markdown
**Описание бага**
Четкое описание проблемы

**Как воспроизвести**
1. Перейти на '...'
2. Нажать на '...'
3. Прокрутить до '...'
4. Увидеть ошибку

**Ожидаемое поведение**
Что должно было произойти

**Скриншоты**
(если применимо)

**Окружение:**
- ОС: [например, Windows 10]
- Браузер: [например, Chrome 120]
- Версия: [например, 2.0.0]

**Дополнительный контекст**
Любая другая информация о проблеме
```

## Предложения функций

### Шаблон предложения:

```markdown
**Описание функции**
Четкое описание желаемой функциональности

**Проблема, которую решает**
Какую проблему решает эта функция

**Предлагаемое решение**
Как вы видите реализацию

**Альтернативы**
Какие альтернативные решения рассматривались

**Дополнительный контекст**
Скриншоты, примеры, ссылки
```

## Приоритеты разработки

### Высокий приоритет:
- 🔴 Критические баги
- 🔴 Проблемы безопасности
- 🟡 Улучшение производительности

### Средний приоритет:
- 🟡 Новые функции
- 🟡 Улучшение UX
- 🟢 Рефакторинг

### Низкий приоритет:
- 🟢 Документация
- 🟢 Косметические изменения
- 🟢 Оптимизация кода

## Области для вклада

### Frontend:
- Улучшение UI/UX
- Новые анимации и эффекты
- Адаптивность
- Доступность (a11y)

### Backend (будущее):
- API разработка
- База данных
- Аутентификация
- Email-сервис

### Документация:
- Улучшение README
- Добавление примеров
- Перевод на другие языки
- Видео-туториалы

### Тестирование:
- Unit тесты
- Integration тесты
- E2E тесты
- Тестирование производительности

## Код поведения

### Наши стандарты:

✅ **Приветствуется:**
- Уважительное общение
- Конструктивная критика
- Помощь новичкам
- Открытость к обратной связи

❌ **Недопустимо:**
- Оскорбления и троллинг
- Дискриминация
- Спам
- Нарушение конфиденциальности

## Лицензия

Внося вклад в проект, вы соглашаетесь, что ваш код будет распространяться под лицензией MIT.

## Вопросы?

Если у вас есть вопросы:

- 📧 Email: info@eventtickets.com
- 💬 GitHub Discussions
- 🐛 GitHub Issues

---

**Спасибо за ваш вклад! 🎉**

Каждый PR, каждый баг-репорт, каждое предложение делает проект лучше!

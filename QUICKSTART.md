# 🚀 Быстрый старт - Деплой на Render за 5 минут

## Шаг 1: Создайте GitHub репозиторий (2 минуты)

1. Перейдите на [github.com](https://github.com) и войдите в аккаунт
2. Нажмите **"New repository"** (зеленая кнопка)
3. Заполните:
   - **Repository name:** `event-tickets` (или любое имя)
   - **Description:** "Современная система продажи билетов"
   - **Public** или **Private** (на ваш выбор)
   - НЕ добавляйте README, .gitignore, license (они уже есть)
4. Нажмите **"Create repository"**

## Шаг 2: Загрузите код на GitHub (1 минута)

Откройте терминал в папке проекта и выполните:

```bash
git init
git add .
git commit -m "Initial commit: EventTickets v2.0"
git branch -M main
git remote add origin https://github.com/ВАШ-USERNAME/event-tickets.git
git push -u origin main
```

**Замените** `ВАШ-USERNAME` на ваш GitHub username!

## Шаг 3: Деплой на Render (2 минуты)

1. Перейдите на [render.com](https://render.com)
2. Войдите через GitHub (или создайте аккаунт)
3. Нажмите **"New +"** → **"Static Site"**
4. Выберите ваш репозиторий `event-tickets`
5. Настройки:
   - **Name:** `eventtickets` (или любое имя)
   - **Branch:** `main`
   - **Build Command:** оставьте пустым
   - **Publish Directory:** `.` (точка)
6. Нажмите **"Create Static Site"**

## ✅ Готово!

Через 1-2 минуты ваш сайт будет доступен по адресу:
```
https://eventtickets.onrender.com
```
(или с вашим именем вместо `eventtickets`)

## 🎉 Что дальше?

### Проверьте работу сайта:
- ✅ Откройте главную страницу
- ✅ Переключите язык (🇷🇺 🇫🇷)
- ✅ Откройте админ-панель: `https://ваш-сайт.onrender.com/admin.html`
- ✅ Проверьте мобильную версию

### Настройте свой домен (опционально):
1. В Render Dashboard → Settings → Custom Domain
2. Добавьте ваш домен (например, `tickets.yourdomain.com`)
3. Настройте DNS записи (Render покажет инструкции)

### Обновление сайта:
После любых изменений в коде:
```bash
git add .
git commit -m "Описание изменений"
git push
```
Render автоматически обновит сайт!

## 🆘 Проблемы?

### Ошибка при git push
```bash
# Если репозиторий уже существует:
git remote set-url origin https://github.com/ВАШ-USERNAME/event-tickets.git
git push -u origin main --force
```

### Сайт не открывается
- Подождите 2-3 минуты после деплоя
- Проверьте логи в Render Dashboard
- Убедитесь, что Publish Directory: `.` (точка)

### Стили не загружаются
- Проверьте, что все файлы загружены на GitHub
- Очистите кэш браузера (Ctrl+Shift+R)

## 📚 Дополнительная информация

- [Полная инструкция по деплою](DEPLOY.md)
- [Руководство пользователя](USAGE_GUIDE.md)
- [Backend интеграция](BACKEND_INTEGRATION.md)

---

**Успешного деплоя! 🚀**
